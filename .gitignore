# Camera config with real credentials — never commit.
# Copy cameras.example.json → cameras.json and fill in your credentials locally.
cameras.json

venv/
__pycache__/
*.pyc
*.pt
evidence/
data/
logs/
.env
output/
input/

runs/

node_modules/
.next/
out/
*.log
.DS_Store
.vercel

# Training data — not used at runtime, must never be committed/deployed.
# Runtime models live in public/models/ and stay tracked.
models/smoking-dataset/
models/security-dataset/

# YAMNet local cache (downloaded by TensorFlow Hub or manually)
models/yamnet_cache/
models/yamnet/
