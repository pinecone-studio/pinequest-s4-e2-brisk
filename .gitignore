# Camera config with real credentials — never commit.
cameras.json

# Local env / secrets
.env
.env.local

# Node / Next.js
node_modules/
.next/
out/
*.tsbuildinfo
tsconfig.tsbuildinfo
*.log
.DS_Store
.vercel

# Runtime evidence snapshots (repo root only — not server/app/api/evidence)
/evidence/
data/
logs/
input/
output/

# Legacy Python / YOLO artifacts (project is TS + Gemini only)
venv/
__pycache__/
*.pyc
*.pt
yolov8n.pt
runs/
